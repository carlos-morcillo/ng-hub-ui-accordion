/**
 * Testing utilities for ng-hub-ui-accordion
 * 
 * @example
 * ```typescript
 * import { AccordionTestUtils } from 'ng-hub-ui-accordion/testing';
 * 
 * describe('AccordionComponent', () => {
 *   it('should handle animations', async () => {
 *     await AccordionTestUtils.waitForAnimation(fixture);
 *     // ... test code
 *   });
 * });
 * ```
 */

export * from './accordion-test-utils';